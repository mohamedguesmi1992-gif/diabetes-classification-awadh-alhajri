# Data

`diabetes_raw.csv` is a **synthetic educational dataset** generated deterministically by the project notebook with a fixed random seed.

It is included only to make the project easier to inspect and reproduce. It does not contain real patient records or protected health information.
